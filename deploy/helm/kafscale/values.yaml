# Copyright 2025-2026 Alexander Alten (novatechflow), NovaTechflow (novatechflow.com).
# This project is supported and financed by Scalytics, Inc. (www.scalytics.io).
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

imagePullSecrets: []

serviceAccount:
  create: true
  name: ""

rbac:
  create: true

operator:
  replicaCount: 1
  image:
    repository: ghcr.io/kafscale/kafscale-operator
    tag: ""
    useLatest: false
    pullPolicy: IfNotPresent
  brokerImage:
    repository: ghcr.io/kafscale/kafscale-broker
    tag: ""
    useLatest: false
  etcdSnapshotEtcdctlImage:
    repository: ghcr.io/kafscale/kafscale-etcd-tools
    tag: ""
    useLatest: false
  etcdReplicas: 3
  etcdStorageSize: ""
  etcdStorageClass: ""
  etcdStorageMemory: false
  etcdEndpoints:
    - "http://etcd:2379"
  leaderKey: "kafscale-operator"
  acl:
    enabled: false
    configJson: ""
    configFile: ""
    failOpen: false
  auth:
    principalSource: ""
    proxyProtocol: false
  podAnnotations: {}
  resources: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # Pod-level security context. PSA-restricted compliant defaults: the
  # operator image runs as USER 10001 (Dockerfile-baked), so we declare
  # runAsNonRoot + fsGroup at the pod level. Override per-environment
  # via `--set operator.podSecurityContext.<key>=<value>` or an
  # environment values file.
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 10001
    runAsGroup: 10001
    fsGroup: 10001
    seccompProfile:
      type: RuntimeDefault
  # Container-level security context. PSA-restricted-required defaults.
  containerSecurityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: ["ALL"]
  metrics:
    enabled: true
    port: 8080
    service:
      annotations: {}
    serviceMonitor:
      enabled: false
      interval: 30s
      scrapeTimeout: 10s
      labels: {}
    prometheusRule:
      enabled: false
      labels: {}

console:
  enabled: true
  replicaCount: 1
  image:
    repository: ghcr.io/kafscale/kafscale-console
    tag: ""
    useLatest: false
    pullPolicy: IfNotPresent
  auth:
    username: ""
    password: null  # Set via --set or use existingSecret
  etcdEndpoints: []
  etcd:
    existingSecret: ""  # Name of existing Secret with ETCD_USERNAME and ETCD_PASSWORD keys
    username: ""
    password: null  # Set via --set or use existingSecret
  metrics:
    brokerMetricsURL: ""
    operatorMetricsURL: ""
  # LFS Dashboard configuration
  lfs:
    enabled: false
    kafkaBrokers: []  # Kafka brokers for consuming tracker events
    trackerTopic: "__lfs_ops_state"
    s3:
      bucket: ""
      region: "us-east-1"
      endpoint: ""
      accessKey: ""
      secretKey: ""
      presignTTL: 300
  podAnnotations: {}
  resources: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  # PSA-restricted compliant defaults. The console image runs as
  # USER 10001 (Dockerfile-baked); pod + container security context
  # declare the four PSA-restricted requirements.
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 10001
    runAsGroup: 10001
    fsGroup: 10001
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: ["ALL"]
  service:
    type: ClusterIP
    port: 80
  ingress:
    enabled: false
    className: ""
    annotations: {}
    hosts:
      - host: console.local
        paths:
          - path: /
            pathType: Prefix
    tls: []

proxy:
  enabled: false
  replicaCount: 2
  image:
    repository: ghcr.io/kafscale/kafscale-proxy
    tag: ""
    useLatest: false
    pullPolicy: IfNotPresent
  health:
    enabled: true
    port: 9094
  # LFS HTTP listener. The proxy binary already serves /lfs/produce,
  # /lfs/download, /lfs/uploads on a separate HTTP port when
  # KAFSCALE_LFS_PROXY_HTTP_ADDR is set. Closing BUG-0008 / iter-7
  # E-15: the chart now wires the env var so clients (kafclaw, etc.)
  # can reach the real LFS endpoint without the bp-001
  # `kafscaleProxyStub` workaround.
  lfs:
    http:
      enabled: false
      port: 8081
      # Service to expose the LFS HTTP port. ClusterIP-only by
      # default; flip to NodePort/LoadBalancer per environment.
      service:
        type: ClusterIP
        port: 8081
  backendCacheTTLSeconds: 60
  advertisedHost: ""
  advertisedPort: 9092
  etcdEndpoints: []
  etcd:
    existingSecret: ""  # Name of existing Secret with ETCD_USERNAME and ETCD_PASSWORD keys
    username: ""
    password: null  # Set via --set or use existingSecret
  backends: []
  podAnnotations: {}
  resources: {}
  nodeSelector: {}
  tolerations: []
  # Default soft anti-affinity: the scheduler prefers to place the
  # two proxy replicas on different nodes. Soft (preferredDuring) so
  # single-node KIND clusters still schedule both replicas; flip to
  # requiredDuring in multi-node production.
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchLabels:
                app.kubernetes.io/component: proxy
                app.kubernetes.io/name: kafscale-proxy
            topologyKey: kubernetes.io/hostname
  # Topology spread complements anti-affinity: the scheduler tries to
  # keep proxy replicas spread evenly across zones (when present).
  # Empty by default; populate when running in a multi-zone cluster.
  topologySpreadConstraints: []
  # PSA-restricted compliant defaults. The proxy image runs as
  # USER 10001 (Dockerfile-baked); pod + container security context
  # declare the four PSA-restricted requirements.
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 10001
    runAsGroup: 10001
    fsGroup: 10001
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: ["ALL"]
  service:
    type: LoadBalancer
    port: 9092
    annotations: {}
    loadBalancerSourceRanges: []

# NOTE: The standalone lfs-proxy has been removed.
# LFS is now a feature-flag on the unified proxy: set proxy.lfs.enabled=true.
# See proxy values above for LFS configuration.

lfsDemos:
  enabled: false
  e72Browser:
    enabled: true  # Browser LFS SDK demo
    lfsProxyEndpoint: ""  # Auto-detected from cluster if empty
    defaultTopic: browser-uploads
    image:
      repository: ghcr.io/kafscale/kafscale-e72-browser-demo
      tag: ""
      useLatest: false
      pullPolicy: IfNotPresent
    service:
      type: NodePort
      port: 80
      nodePort: 30072
    ingress:
      enabled: false
      className: ""
      annotations: {}
      hosts:
        - host: lfs-demo.local
          paths:
            - path: /
              pathType: Prefix
      tls: []

mcp:
  enabled: false
  namespace:
    name: kafscale-mcp
    create: true
  replicaCount: 1
  image:
    repository: ghcr.io/kafscale/kafscale-mcp
    tag: ""
    useLatest: false
    pullPolicy: IfNotPresent
  serviceAccount:
    create: true
    name: ""
  auth:
    token: ""
  etcdEndpoints: []
  etcd:
    existingSecret: ""  # Name of existing Secret with ETCD_USERNAME and ETCD_PASSWORD keys
    username: ""
    password: null  # Set via --set or use existingSecret
  metrics:
    brokerMetricsURL: ""
  sessionTimeout: ""
  podAnnotations: {}
  resources: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  service:
    type: ClusterIP
    port: 80
  ingress:
    enabled: false
    className: ""
    annotations: {}
    hosts:
      - host: mcp.local
        paths:
          - path: /
            pathType: Prefix
    tls: []
